#!/bin/bash

curr_dir=$(dirname $(readlink -f `which baseline`))

command=$1
shift

command_file="$curr_dir"/../lib/baseline-$command

if ! [[ -x $command_file ]]; then
  echo "Command does not exist. Run baseline help"
  exit 1
fi

exec $command_file $@
